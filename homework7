1- select rating, COUNT(rating) from film group by rating
2- select replacement_cost, COUNT(*) from film group by replacement_cost HAVING COUNT(*)  > 50
3-select store_id, COUNT (*) from customer Group BY store_id
4- select country_id, Count(city) from city group by country_id ORDER BY country_id DESC LIMIT 1
